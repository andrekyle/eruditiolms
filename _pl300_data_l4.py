# Auto-generated by _build_pl300.py
LESSON_HTML = """
<h2>Lesson 4: Model the Data</h2>

<h3>Understanding Star Schema Architecture</h3>
<p>Effective Power BI models follow a <strong>star schema</strong> architecture, which separates data into two types of tables. <em>Fact tables</em> contain transactional data with quantifiable metrics (sales amounts, quantities, counts), while <em>dimension tables</em> contain descriptive attributes (customer names, product categories, dates). Fact tables have foreign keys linking to dimensions, creating the "star" pattern with the fact table at the center. This structure optimizes query performance and simplifies business logic.</p>

<h3>Cardinality and Relationships</h3>
<p>Cardinality defines the relationship type between tables. The four primary cardinality patterns are:</p>
<ul>
<li><strong>One-to-Many (1:*):</strong> The most common and efficient pattern. One dimension row relates to multiple fact rows (e.g., one customer has many orders).</li>
<li><strong>Many-to-One (*:1):</strong> The reverse of one-to-many; typically the fact table side of a 1:* relationship.</li>
<li><strong>One-to-One (1:1):</strong> Each row in one table relates to exactly one row in another. Use sparingly; consider merging tables instead.</li>
<li><strong>Many-to-Many (*:*):</strong> Multiple rows on both sides relate to multiple rows on the other. Requires a bridge table to implement properly.</li>
</ul>

<h3>Cross-Filter Direction</h3>
<p>Cross-filter direction controls which direction filters propagate through relationships:</p>
<ul>
<li><strong>Single direction:</strong> Filters flow from dimension to fact table (the standard, safe default). A date filter affects sales, but sales never filter dates.</li>
<li><strong>Both directions (bidirectional):</strong> Filters propagate in both directions. This is powerful but risky—it can cause unexpected results, circular dependencies, and performance degradation. Use only when absolutely necessary and with full understanding of consequences.</li>
</ul>

<h3>Active and Inactive Relationships</h3>
<p>Each pair of tables can have multiple relationships, but only one is <strong>active</strong> by default. Active relationships automatically participate in filters and DAX queries. <strong>Inactive relationships</strong> exist but don't participate automatically; you must reference them explicitly using the <code>USERELATIONSHIP</code> function in DAX. This enables role-playing dimensions—multiple relationships from the same table serving different purposes (e.g., Order Date vs. Ship Date from a single Date table).</p>

<p>Example with role-playing dates:</p>
<pre><code class="language-dax">OrdersByMonth := 
CALCULATE(
    SUM(Sales[Amount]),
    USERELATIONSHIP(Dates[DateKey], Sales[OrderDateKey])
)
</code></pre>

<h3>Calculated Columns vs. Measures</h3>
<table>
<tr>
<th>Aspect</th>
<th>Calculated Column</th>
<th>Measure</th>
</tr>
<tr>
<td><strong>Computation</strong></td>
<td>Evaluated once per row during data refresh</td>
<td>Evaluated in context for each cell in a visual</td>
</tr>
<tr>
<td><strong>Storage</strong></td>
<td>Results stored in the model; increases size</td>
<td>Not stored; calculated on-demand; smaller footprint</td>
</tr>
<tr>
<td><strong>Filters</strong></td>
<td>Can be used in filters and row context</td>
<td>Respond dynamically to filters</td>
</tr>
<tr>
<td><strong>Use Case</strong></td>
<td>Simple transformations (concatenation, basic arithmetic, lookups)</td>
<td>Aggregations, complex logic, percent-of-total, year-over-year</td>
</tr>
</table>

<p>Use calculated columns for dimensions (categories, flags) and measures for analytics (sums, averages, ratios). Overusing calculated columns inflates model size without performance benefit.</p>

<h3>Hierarchies and Date Tables</h3>
<p>Create <strong>hierarchies</strong> in dimension tables to enable drill-down navigation. A geography hierarchy (Country → Region → City) or product hierarchy (Category → Subcategory → Product) improves user experience in visuals.</p>

<p><strong>Mark as Date Table:</strong> Always designate a dedicated date table using Power BI's "Mark as Date Table" option. This enables time intelligence functions like YEAR-TO-DATE, MONTH-OVER-MONTH, and SAME PERIOD LAST YEAR. Ensure it contains every date in your fact tables with no gaps.</p>

<h3>Optimizing Model Size</h3>
<p>Large models slow refresh times and increase memory usage:</p>
<ul>
<li>Remove unused columns and relationships before loading.</li>
<li>Use appropriate data types (text instead of unnecessary integer columns).</li>
<li>Prefer measures over calculated columns; avoid redundant calculations.</li>
<li>Apply column compression and indexing where appropriate.</li>
<li>Audit cardinality—high-cardinality foreign keys increase size dramatically.</li>
</ul>

<h3>Composite Models</h3>
<p>Composite models combine DirectQuery and Import storage modes in a single dataset. Some tables import data directly; others query the source in real-time. This approach balances responsiveness with data freshness but adds complexity. Use for scenarios where some dimensions benefit from caching while others must reflect current state.</p>
"""

QUESTIONS = [
    ("multiple_choice", "&lt;p&gt;Which type of table in a star schema contains quantifiable metrics like revenue and order count?&lt;/p&gt;", [("&lt;em&gt;Dimension table&lt;/em&gt;", False), ("&lt;em&gt;Fact table&lt;/em&gt;", True), ("&lt;em&gt;Bridge table&lt;/em&gt;", False), ("&lt;em&gt;Lookup table&lt;/em&gt;", False)], "A fact table stores transactional measures and metrics (sales, quantities), while dimensions describe attributes."),
    ("multiple_choice", "&lt;p&gt;You have a &amp;quot;Products&amp;quot; table with 10,000 rows that will be used to filter sales by category. Should you create a &lt;strong&gt;calculated column&lt;/strong&gt; or a &lt;strong&gt;measure&lt;/strong&gt; to group products into brands?&lt;/p&gt;", [("Measure, because it dynamically responds to filters", False), ("Calculated column, because it stores the brand grouping once at refresh and reduces query time", True), ("Either; they produce identical results", False), ("Neither; use a filter in the visual instead", False)], "Calculated columns are ideal for dimension attributes because results are stored once and don&apos;t need recalculation per visual cell."),
    ("multiple_choice", "&lt;p&gt;In a one-to-many relationship between Customers (1) and Orders (*), where filters typically propagate from Customers to Orders, what is the filter &lt;strong&gt;cardinality&lt;/strong&gt;?&lt;/p&gt;", [("Many-to-many", False), ("One-to-one", False), ("One-to-many", True), ("Bidirectional", False)], "One-to-many (1:*) means one customer row relates to many order rows. This is the default and most efficient cardinality."),
    ("multiple_choice", "&lt;p&gt;Your report filters sales by a region slicer and also filters the Date table by a date slicer. You notice unexpected behavior where changing sales dates unexpectedly changes available regions. What is the likely cause?&lt;/p&gt;", [("The date table is incorrectly marked", False), ("Bidirectional cross-filter is enabled on the relationship between regions and sales", True), ("The fact table has duplicate keys", False), ("Calculated columns are stored incorrectly", False)], "Bidirectional filters allow unexpected filter propagation. Default single-direction filtering prevents region filters from being affected by date selections."),
    ("multiple_choice", "&lt;p&gt;You have a Dates table related to Sales via &lt;strong&gt;OrderDate&lt;/strong&gt;. You also need to analyze by &lt;strong&gt;ShipDate&lt;/strong&gt; using the same Dates table. How do you set this up?&lt;/p&gt;", [("Delete the first relationship and create a new one for ShipDate", False), ("Create a second relationship to ShipDate and mark it &lt;em&gt;inactive&lt;/em&gt;, then use &lt;code&gt;USERELATIONSHIP()&lt;/code&gt; in measures", True), ("Create separate Date tables for each date type", False), ("Use a bidirectional filter on both relationships", False)], "This is a role-playing dimension pattern. The inactive relationship is activated in specific measures using USERELATIONSHIP() for context-dependent calculations."),
    ("multiple_choice", "&lt;p&gt;What is the primary benefit of marking a table as a Date Table in Power BI?&lt;/p&gt;", [("It reduces model file size by 50%", False), ("It enables time intelligence functions like YEAR-TO-DATE and MONTH-OVER-MONTH", True), ("It automatically removes duplicate dates", False), ("It prevents bidirectional filters", False)], "Marking as Date Table unlocks Power BI&apos;s time intelligence DAX functions, which rely on a properly structured calendar table."),
    ("multiple_choice", "&lt;p&gt;Your model has 500,000 rows in the Sales fact table and a Product dimension with 50,000 rows. Sales join to Products via a ProductID. What is the primary risk of this high-cardinality relationship?&lt;/p&gt;", [("Filters will always be bidirectional", False), ("Model size increases significantly because the relationship requires more memory overhead", True), ("Date table functionality breaks", False), ("Calculated columns will not work", False)], "High-cardinality joins consume proportionally more memory. Audit whether all 50,000 products are actually used in Sales before including them."),
    ("multiple_choice", "&lt;p&gt;You are optimizing a slow-performing model. Which action will definitively reduce model size without losing functionality?&lt;/p&gt;", [("Convert all measures to calculated columns", False), ("Remove unused columns and relationships that do not appear in any visual or calculation", True), ("Enable bidirectional filters on all relationships", False), ("Split the model into separate Power BI files", False)], "Unused columns and relationships waste memory. Removing them reduces refresh time and model footprint with no loss of intended analytics capability.")
]
