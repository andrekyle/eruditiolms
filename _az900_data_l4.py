# Auto-generated by _build_az900.py
LESSON_HTML = """
<h2>Lesson 4: Azure Architecture and Core Services</h2>

<h3>Introduction to Azure Architecture</h3>
<p>Microsoft Azure's architecture is built on a sophisticated hierarchy of organizational and physical components that enable global service delivery, high availability, and disaster recovery. Understanding this architecture is fundamental to designing resilient, scalable cloud solutions. This lesson covers the core architectural components that form the backbone of Azure's infrastructure and resource management model.</p>

<h3>Azure Regions and Geographic Distribution</h3>
<p>Azure operates <strong>multiple geographical regions</strong> across the world, each containing multiple datacenters. A region is a set of datacenters deployed within a latency-defined perimeter and connected through a dedicated regional low-latency network. Regions enable you to deploy applications geographically closer to your users, reducing latency and meeting data residency requirements.</p>

<p>Azure currently operates in over 60 regions worldwide, making it the most widely distributed cloud provider. Each region is designed to be completely independent, with its own power supply, cooling systems, and networking infrastructure. This independence ensures that a failure in one region does not impact other regions.</p>

<h4>Region Pairs</h4>
<p>Most Azure regions are paired with another region at least 300 miles away within the same geographic area. <strong>Region pairs</strong> provide a critical mechanism for disaster recovery and business continuity. When Azure applies platform updates, it staggers them across paired regions to minimize downtime. If one region experiences a widespread outage, services automatically fail over to the paired region.</p>

<p>Region pairs offer the following benefits:</p>
<ul>
<li><strong>Automatic failover:</strong> Some Azure services can automatically replicate data to paired regions.</li>
<li><strong>Planned maintenance:</strong> Azure updates are coordinated so only one region in a pair is updated at a time.</li>
<li><strong>Data residency:</strong> Pairs typically remain within the same geography for compliance requirements.</li>
<li><strong>Sequential recovery:</strong> In a widespread outage, recovery is prioritized for one region per pair.</li>
</ul>

<h4>Sovereign Regions</h4>
<p><strong>Sovereign regions</strong> are isolated instances of Azure deployed for specific government or compliance requirements. These regions are physically and logically isolated from the public Azure environment and operated by vetted personnel within those countries.</p>

<p><strong>Azure Government regions</strong> (US Government, US Government Secret, US Government Top Secret) are dedicated to US federal, state, local, and tribal governments and their partners. These regions comply with FedRAMP (Federal Risk and Authorization Management Program) standards and use US-only personnel.</p>

<p><strong>Azure China regions</strong> (China East, China North) are operated by 21Vianet in partnership with Microsoft. Chinese data residency laws require that data remains within China, and these sovereign regions ensure compliance with those requirements. They are not directly connected to global Azure regions.</p>

<h3>Availability Zones</h3>
<p><strong>Availability Zones</strong> are physically separate datacenters within an Azure region that are connected by high-speed fiber networks. Each availability zone has independent power, cooling, and networking infrastructure. They are typically 2-3 kilometers apart, close enough for low-latency replication but far enough to avoid simultaneous failures.</p>

<p>Availability Zones enable two important architectural patterns:</p>

<ul>
<li><strong>Zonal services:</strong> You explicitly pin resources (Virtual Machines, Managed Disks, IP addresses) to specific zones. This gives you control over resilience strategy for critical applications.</li>
<li><strong>Zone-redundant services:</strong> Azure automatically replicates data across availability zones. Services like Storage, SQL Database, and Azure Load Balancer can be configured for zone-redundancy, providing automatic failover without intervention.</li>
</ul>

<p>Not all Azure regions have availability zones. As of 2026, approximately 45+ regions support availability zones. When designing mission-critical applications, verifying zone availability in your target region is essential.</p>

<h3>Azure Datacenters</h3>
<p>Azure datacenters are the physical facilities that house compute, storage, and networking hardware. Each datacenter is a standalone entity within an availability zone, fully powered, cooled, and networked. Microsoft operates over 200 datacenters globally, with continuous investment in expanding capacity.</p>

<p>Key characteristics of Azure datacenters:</p>
<ul>
<li><strong>Security:</strong> Biometric access controls, surveillance, and multiple layers of physical security.</li>
<li><strong>Redundancy:</strong> Multiple power feeds, backup generators, and UPS systems ensure continuous operation.</li>
<li><strong>Automation:</strong> Robotic arms and automated systems manage hardware, reducing manual intervention.</li>
<li><strong>Monitoring:</strong> 24/7 monitoring and predictive maintenance prevent hardware failures.</li>
<li><strong>Sustainability:</strong> Advanced cooling systems and renewable energy usage reduce environmental impact.</li>
</ul>

<h3>Azure Resources</h3>
<p>An <strong>Azure resource</strong> is any manageable item that is available through Azure. Resources include Virtual Machines, App Services, SQL databases, Storage accounts, Virtual Networks, Managed Disks, and hundreds of other services. Each resource is an instance of a service that you can create, configure, and manage.</p>

<p>Resources have the following characteristics:</p>
<ul>
<li><strong>Unique resource ID:</strong> Every resource has a unique identifier within its scope.</li>
<li><strong>Location:</strong> Resources are deployed to specific regions and availability zones.</li>
<li><strong>Tags:</strong> Key-value pairs that organize and manage resources for billing, access control, and automation.</li>
<li><strong>Cost:</strong> Resources generate usage charges based on type, size, and runtime.</li>
<li><strong>Lifecycle:</strong> Resources can be created, updated, and deleted through Azure Portal, CLI, PowerShell, or ARM templates.</li>
</ul>

<h3>Resource Groups</h3>
<p>A <strong>resource group</strong> is a container that holds related resources for an Azure solution. All resources must belong to a resource group. Resource groups serve as a logical boundary for:</p>

<ul>
<li><strong>Organization:</strong> Group resources by project, environment, or business unit.</li>
<li><strong>Access Control (RBAC):</strong> Assign permissions at the resource group level; members inherit permissions for all contained resources.</li>
<li><strong>Billing:</strong> Track costs for all resources within a group.</li>
<li><strong>Lifecycle management:</strong> Delete a resource group to remove all contained resources simultaneously.</li>
<li><strong>Deployment:</strong> Deploy infrastructure as code (Infrastructure as Code / IaC) to provision entire groups at once.</li>
</ul>

<p>A resource group is a collection that lives in exactly one region, though resources within that group can span multiple regions and availability zones. The region assignment helps organize management and billing.</p>

<h3>Subscriptions</h3>
<p>An <strong>Azure subscription</strong> is a logical grouping of Azure services that links your Azure account to your billing account. Each subscription has its own billing, resource quotas, and service limits. You must have at least one subscription to create and use Azure resources.</p>

<p>Subscriptions serve multiple purposes:</p>

<ul>
<li><strong>Billing boundary:</strong> Each subscription receives a separate invoice and cost breakdown.</li>
<li><strong>Quota boundary:</strong> Subscriptions have default limits on resource counts and vCore usage, which can be increased via support requests.</li>
<li><strong>Access boundary:</strong> Resource access is controlled independently for each subscription.</li>
<li><strong>Organizational separation:</strong> Use multiple subscriptions for different departments, projects, or environments (Dev, Test, Production).</li>
</ul>

<p>An organization can have multiple subscriptions under a single Azure Active Directory (Microsoft Entra ID) tenant. This enables centralized identity management while maintaining separate billing and quotas.</p>

<h3>Management Groups</h3>
<p><strong>Management groups</strong> are containers that help you manage access, compliance, and spending across multiple subscriptions. Management groups enable you to enforce policies, apply role-based access control, and organize subscriptions at scale.</p>

<p>Key features of management groups:</p>

<ul>
<li><strong>Hierarchical organization:</strong> Management groups can be nested up to six levels deep.</li>
<li><strong>Policy enforcement:</strong> Azure Policy definitions assigned at a management group automatically apply to all contained subscriptions.</li>
<li><strong>Unified access control:</strong> Assign RBAC roles at the management group level; all subscriptions inherit the assignment.</li>
<li><strong>Cost analysis:</strong> View aggregated costs across multiple subscriptions.</li>
<li><strong>Compliance reporting:</strong> Generate unified compliance reports across the organization.</li>
</ul>

<p>A tenant can contain up to 10,000 management groups. The root management group (the top level) is automatically created and cannot be deleted. Organizations can structure management groups to reflect their organizational structure, business units, or compliance domains.</p>

<h3>Azure Organizational Hierarchy</h3>
<p>The Azure organizational hierarchy defines how resources, subscriptions, and management are structured. Understanding this hierarchy is critical for designing secure, scalable cloud governance.</p>

<table>
<thead>
<tr>
<th>Level</th>
<th>Component</th>
<th>Purpose</th>
<th>Scope Example</th>
<th>Key Characteristics</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>Management Group</td>
<td>Organize subscriptions for policy and access control</td>
<td>Enterprise, Finance Division, Production Environment</td>
<td>Hierarchical (up to 6 levels); applies policies recursively; inherited RBAC roles</td>
</tr>
<tr>
<td>2</td>
<td>Subscription</td>
<td>Billing boundary, quota limits, resource container</td>
<td>Production-US, Development-EU, Pilot-PoC</td>
<td>One per payment method; contains resource groups; independent quotas</td>
</tr>
<tr>
<td>3</td>
<td>Resource Group</td>
<td>Logical container for related resources</td>
<td>rg-webapp-prod, rg-database-backup, rg-networking</td>
<td>Must exist in one region; can span multiple zones; billing unit; lifecycle container</td>
</tr>
<tr>
<td>4</td>
<td>Resource</td>
<td>Individual Azure service instance</td>
<td>VM, App Service, SQL Database, Storage Account</td>
<td>Created in specific zones/regions; assigned unique ID; tagged for organization</td>
</tr>
</tbody>
</table>

<h3>Regions, Region Pairs, Availability Zones, and Sovereign Regions Comparison</h3>

<table>
<thead>
<tr>
<th>Aspect</th>
<th>Azure Region</th>
<th>Region Pair</th>
<th>Availability Zone</th>
<th>Sovereign Region</th>
</tr>
</thead>
<tbody>
<tr>
<td>Physical Scope</td>
<td>Multiple datacenters in a geographic area</td>
<td>Two regions at least 300 miles apart</td>
<td>Independent datacenter within a region (2-3 km apart)</td>
<td>Isolated Azure deployment for government/compliance</td>
</tr>
<tr>
<td>Primary Use</td>
<td>Geographic proximity to users; data residency</td>
<td>Disaster recovery; business continuity</td>
<td>High availability; fault tolerance within a region</td>
<td>Regulatory compliance; sovereign data control</td>
</tr>
<tr>
<td>Data Replication</td>
<td>Not automatic; requires explicit setup</td>
<td>Automatic for some services; synchronous</td>
<td>Automatic for zone-redundant services; synchronous</td>
<td>Within sovereign boundaries only; no cross-border replication</td>
</tr>
<tr>
<td>Latency</td>
<td>Low latency within region</td>
<td>Higher latency between regions</td>
<td>Very low latency between zones (sub-millisecond)</td>
<td>Determined by physical infrastructure</td>
</tr>
<tr>
<td>Availability SLA</td>
<td>99.9% or higher (service-dependent)</td>
<td>99.95% with proper architecture</td>
<td>99.99% with zone-redundancy (4+ nines)</td>
<td>Service-dependent; often higher for critical services</td>
</tr>
<tr>
<td>Examples</td>
<td>East US, North Europe, Southeast Asia</td>
<td>East US ↔ West US; UK South ↔ UK West</td>
<td>Zone 1, Zone 2, Zone 3 within East US</td>
<td>Azure Government (US); Azure China (21Vianet)</td>
</tr>
</tbody>
</table>

<h3>Practical Deployment Patterns</h3>

<h4>Creating a Resource Group (Azure CLI)</h4>
<pre><code class="language-bash">
# Create a resource group in East US
az group create \
  --name rg-webapp-prod \
  --location eastus

# Create a resource group in paired region (West US) for disaster recovery
az group create \
  --name rg-webapp-dr \
  --location westus
</code></pre>

<h4>Deploying Zone-Redundant Resources (ARM Template Example)</h4>
<pre><code class="language-json">
{
  "type": "Microsoft.Compute/virtualMachines",
  "apiVersion": "2023-03-01",
  "name": "vm-app-01",
  "location": "eastus",
  "zones": ["1", "2", "3"],
  "properties": {
    "hardwareProfile": {
      "vmSize": "Standard_D4s_v5"
    },
    "osProfile": {
      "computerName": "vm-app-01",
      "adminUsername": "azureuser",
      "adminPassword": "[parameters('adminPassword')]"
    }
  }
}
</code></pre>

<h4>Tagging Resources for Organization and Cost Tracking</h4>
<pre><code class="language-json">
{
  "tags": {
    "Environment": "Production",
    "CostCenter": "Finance-001",
    "Project": "ERP-Migration",
    "Owner": "alice@contoso.com",
    "BackupPolicy": "Daily",
    "ComplianceLevel": "Sensitive"
  }
}
</code></pre>

<h3>Designing for High Availability</h3>
<p>When designing Azure solutions, you should implement resources across multiple availability zones and, for critical systems, multiple regions. A typical architecture includes:</p>

<ul>
<li><strong>Single region with zone-redundancy:</strong> For most applications requiring high availability (99.99% SLA).</li>
<li><strong>Multi-region with active-active:</strong> For mission-critical applications requiring disaster recovery and lowest possible latency.</li>
<li><strong>Multi-region with active-passive:</strong> For applications where failover to a secondary region is acceptable but primary region is preferred.</li>
</ul>

<p>Management groups and subscriptions provide the governance framework to enforce these architectural decisions across your organization.</p>

<h3>Key Takeaways</h3>
<ul>
<li>Azure operates 60+ regions globally, with region pairs providing automatic disaster recovery coordination.</li>
<li>Sovereign regions (Government, China) are isolated for compliance with national regulations.</li>
<li>Availability zones enable zone-redundancy and zonal deployments within regions for 99.99% SLA.</li>
<li>Resource groups are the logical container for related resources and form the basis for RBAC and lifecycle management.</li>
<li>Subscriptions define billing boundaries, quotas, and access control boundaries.</li>
<li>Management groups enable organization-wide policy enforcement and RBAC at scale.</li>
<li>The organizational hierarchy (Management Group → Subscription → Resource Group → Resource) reflects Azure's design for enterprise governance.</li>
</ul>
"""

QUESTIONS = [
    (
        "multiple_choice",
        "Your organization needs to deploy a web application with a Service Level Agreement (SLA) of 99.99% uptime. The application must remain available even if a single Azure datacenter fails. Which combination of Azure architectural components best achieves this requirement?",
        [
            ("Deploy resources across multiple subscriptions in the same region", 0),
            ("Deploy resources across multiple availability zones in the same region using zone-redundant services", 1),
            ("Deploy all resources in a single availability zone for cost optimization", 0),
            ("Deploy resources only in Azure Government regions", 0),
        ],
        "Zone-redundant services automatically replicate data across multiple availability zones within a region, providing 99.99% SLA. A single availability zone cannot meet this SLA. Multiple subscriptions provide billing separation, not redundancy. Azure Government is for compliance, not availability."
    ),
    (
        "multiple_choice",
        "You manage a multinational organization with operations in the USA and Europe. Regulatory requirements mandate that US customer data remains in the US and European customer data remains in Europe. How should you structure your Azure subscriptions and regions to meet these compliance requirements?",
        [
            ("Use a single subscription with resources in East US and West Europe regions", 0),
            ("Create separate subscriptions and deploy to region pairs: East US paired with West US for US data, and North Europe paired with West Europe for EU data", 1),
            ("Deploy all data to Azure Government regions only", 0),
            ("Use management groups to automatically separate data by region", 0),
        ],
        "Region pairs maintain data within the same geography. Separate subscriptions provide isolation for different data residency requirements. Azure Government is only for US government use. Management groups organize access control and policy, not data residency."
    ),
    (
        "multiple_choice",
        "Your team is designing the resource structure for a new project. You need to deploy 45 related resources across three environments (Development, Staging, Production). Which organizational approach best aligns with Azure's recommended practices?",
        [
            ("Create a single resource group containing all 45 resources for easy management", 0),
            ("Create separate resource groups for each environment: rg-dev, rg-staging, rg-prod, with resources organized by environment", 1),
            ("Create a separate subscription for each resource", 0),
            ("Create a management group for each resource", 0),
        ],
        "Resource groups should contain related resources in a specific environment. Separate resource groups by environment enable independent access control, lifecycle management, and cost tracking. Individual subscriptions/management groups per resource would create unnecessary organizational complexity."
    ),
    (
        "multiple_choice",
        "Your enterprise operates across 12 business units with different compliance requirements. You want to enforce specific Azure policies uniformly across all business units while allowing each unit to manage its own resources. Which Azure organizational component is best suited for this requirement?",
        [
            ("Create a single resource group for all business units", 0),
            ("Create a management group for each business unit and assign policies at the parent management group level", 1),
            ("Create a separate subscription for each resource", 0),
            ("Deploy resources only to sovereign regions", 0),
        ],
        "Management groups enable hierarchical policy enforcement. Policies assigned to a parent management group automatically apply to all child management groups and subscriptions. Resource groups cannot enforce organization-wide policies. Subscriptions per resource creates chaos. Sovereign regions address compliance, not policy management."
    ),
    (
        "multiple_choice",
        "Which of the following statements accurately describes the relationship between Azure regions and region pairs?",
        [
            ("Region pairs are located in the same physical datacenter for maximum redundancy", 0),
            ("Region pairs are at least 300 miles apart within the same geography and coordinate platform updates to minimize downtime", 1),
            ("Region pairs are only used for connecting to Azure Government regions", 0),
            ("Region pairs are identical duplicates of each other and cannot function independently", 0),
        ],
        "Region pairs are geographically separated (300+ miles) but within the same geography. They coordinate platform updates so only one region is updated at a time. Pairs are independent and can function separately. They are not related to Government regions or physical colocation."
    ),
    (
        "multiple_choice",
        "A multinational corporation must comply with China's data residency laws requiring all customer data to be stored in mainland China. Which Azure deployment option meets this requirement?",
        [
            ("Deploy to East US region with manual replication to China", 0),
            ("Deploy to Azure Government regions in the US", 0),
            ("Deploy to Azure China regions operated by 21Vianet in partnership with Microsoft", 1),
            ("Deploy to any Azure region and use encryption to satisfy compliance", 0),
        ],
        "Azure China regions (operated by 21Vianet) are the only way to meet China's data residency requirements. Public Azure regions do not meet these requirements. Azure Government serves US federal agencies. Encryption alone does not satisfy data residency mandates."
    ),
    (
        "multiple_choice",
        "Your application uses an Azure SQL Database configured with zone-redundant deployment across Availability Zone 1, 2, and 3 in the East US region. If Availability Zone 1 fails completely, what happens to your application?",
        [
            ("Application becomes completely unavailable and you must manually failover to a backup", 0),
            ("Application experiences a brief interruption while Azure automatically fails over to remaining zones with no data loss", 1),
            ("Application continues operating with no interruption as Azure automatically redirects traffic to remaining zones", 0),
            ("Application must be migrated to a different region immediately", 0),
        ],
        "Zone-redundant services automatically failover between zones with no data loss. A brief interruption typically occurs (milliseconds to seconds) during failover. No manual intervention is required. Data is replicated synchronously across zones, ensuring no data loss. Migration to another region is only necessary if multiple zones fail."
    ),
    (
        "multiple_choice",
        "An organization decides to consolidate its Azure footprint by combining Development, Staging, and Production resources into a single resource group to reduce management overhead. What is the primary disadvantage of this approach?",
        [
            ("It prevents the organization from using Azure policy", 0),
            ("It eliminates the ability to apply different access control policies to each environment and makes it difficult to track costs and lifecycle management by environment", 1),
            ("It requires additional management groups to function", 0),
            ("It automatically deletes resources in other environments if one is deleted", 0),
        ],
        "Combining environments in one resource group prevents applying environment-specific RBAC, makes cost tracking difficult, and risks accidental deletion of production resources. Azure policy still functions but is less granular. Management groups are independent of resource group organization. Deletion of one environment only affects resources in that group if explicitly done."
    ),
]
