# Auto-generated by _build_pl300.py
LESSON_HTML = """
<h2>Lesson 3: Prepare the Data</h2>

<h3>Understanding Data Sources in Power BI</h3>
<p>The foundation of any Power BI solution begins with connecting to and preparing data. Power BI supports a wide range of data sources, enabling you to integrate data from multiple systems. Common data sources include <strong>Excel</strong> files for local analysis, <strong>CSV</strong> files for structured text data, <strong>SQL Server</strong> and <strong>Azure SQL Database</strong> for enterprise relational data, <strong>SharePoint</strong> lists for collaborative environments, <strong>Dataverse</strong> for model-driven applications, <strong>Web</strong> data via APIs, and <strong>Dataflows</strong> for reusable data preparation pipelines. Understanding which source best fits your scenario is critical for building efficient solutions.</p>

<h3>Storage Modes and Their Trade-Offs</h3>
<p>Power BI offers four storage modes, each with distinct characteristics and performance implications. Your choice affects both report interactivity and data refresh requirements:</p>

<table border="1" cellpadding="8" cellspacing="0">
<tr>
<th>Storage Mode</th>
<th>Best For</th>
<th>Data Refresh</th>
<th>Query Performance</th>
</tr>
<tr>
<td><strong>Import</strong></td>
<td>Local analysis, fast queries, smaller datasets</td>
<td>Scheduled refresh</td>
<td>Fastest (in-memory)</td>
</tr>
<tr>
<td><strong>DirectQuery</strong></td>
<td>Real-time data, large datasets, compliance requirements</td>
<td>Always current</td>
<td>Slower (source dependent)</td>
</tr>
<tr>
<td><strong>Dual</strong></td>
<td>Balanced approach with local aggregations</td>
<td>Scheduled + real-time</td>
<td>Mixed (in-memory + source)</td>
</tr>
<tr>
<td><strong>Live Connection</strong></td>
<td>Analysis Services, Power BI datasets</td>
<td>Data owner's refresh</td>
<td>Source dependent</td>
</tr>
</table>

<p><em>Import mode</em> loads data into Power BI's in-memory engine, providing lightning-fast query performance and full feature support. However, it requires scheduled refreshes and is limited by dataset size. <em>DirectQuery</em> sends queries directly to the source, ensuring real-time data but potentially slower performance. <em>Dual mode</em> allows Power BI to intelligently choose between in-memory aggregations and source queries. <em>Live Connection</em> connects directly to Analysis Services or other Power BI datasets without importing data locally.</p>

<h3>The Power Query Editor</h3>
<p>The Power Query Editor is where data transformation happens. This visual interface allows you to apply cleaning, shaping, and enrichment operations without writing code. When you connect to a data source, Power Query automatically generates M language formulas behind every operation you perform. You can view, edit, and even write M code directly when needed. The editor displays your data in a preview pane, applied steps in a side panel, and formula bar at the top, creating an intuitive workflow for data preparation.</p>

<h3>Data Profiling and Quality Assessment</h3>
<p>Before transforming data, analyze its quality and characteristics. Power Query's <strong>column quality</strong> indicators show the percentage of valid, null, and error values in each column. The <strong>column distribution</strong> view displays the frequency and variety of values, helping identify outliers and patterns. The <strong>column profile</strong> provides detailed statistics including unique count, type distribution, and numeric ranges. These profiling features help you make informed decisions about which data requires cleaning and which transformations are appropriate.</p>

<h3>Shaping and Cleaning Data</h3>
<p>Data shaping transforms your raw data into the structure required for analysis. Key operations include:</p>
<ul>
<li><strong>Promote Headers:</strong> Use the first row as column names when data sources lack proper headers.</li>
<li><strong>Change Data Types:</strong> Ensure columns are typed correctly (text, number, date, etc.) for accurate analysis and calculations.</li>
<li><strong>Remove Duplicates:</strong> Eliminate duplicate rows to ensure each record represents a unique entity.</li>
<li><strong>Replace Values:</strong> Standardize inconsistent entries (e.g., converting "Yes"/"Y"/"1" to a uniform format).</li>
<li><strong>Split/Merge Columns:</strong> Break combined data into separate columns or consolidate related columns for better organization.</li>
<li><strong>Unpivot:</strong> Convert wide formats (where attributes are columns) into tall formats where each row represents a single fact. This is essential for normalized data structures.</li>
<li><strong>Append Queries:</strong> Combine tables vertically by stacking rows from multiple sources with matching column structures. Use this when consolidating similar datasets across regions or time periods.</li>
<li><strong>Merge Queries:</strong> Combine tables horizontally using common key columns, similar to SQL joins. Choose merge type based on your analysis needs (inner, left, right, full outer, cross join).</li>
<li><strong>Group By:</strong> Aggregate data by creating summary rows, calculating totals, averages, counts, or other statistics grouped by specified columns.</li>
</ul>

<h3>Query Parameters and Query Folding</h3>
<p><strong>Query parameters</strong> are reusable values that can be referenced across multiple queries. Use parameters to create dynamic solutions where changing a single parameter automatically updates all dependent queries. This is especially useful for filtering by date ranges, selecting regions, or switching between development and production data sources. When creating parameters, specify their data type and default values for consistency.</p>

<p><strong>Query folding</strong> occurs when Power Query optimizes transformations by pushing operations back to the source system rather than processing them in Power BI. This dramatically improves performance because the source database handles filtering, sorting, and aggregation before returning only the necessary results. Not all operations support folding—custom functions and certain transformations force Power Query to load full datasets locally. To verify folding, right-click a step in Applied Steps and select "View Native Query" if available. When folding is possible, you'll see the generated SQL; when it breaks, that option will be grayed out. Always verify folding is occurring when working with large datasets via DirectQuery or when performance is critical.</p>

<p>A practical example of query folding:</p>
<pre><code class="language-powerquery">let
    Source = Sql.Database("server_name", "database_name"),
    Sales = Source{[Schema="dbo", Item="Sales"]}[Data],
    FilteredYear = Table.SelectRows(Sales, each [Year] = 2024),
    FilteredRegion = Table.SelectRows(FilteredYear, each [Region] = "North")
in
    FilteredRegion
</code></pre>

<p>In this example, both the year and region filters fold back to the database, which executes them in SQL before returning results to Power BI—significantly reducing data transfer and improving responsiveness.</p>
"""

QUESTIONS = [("multiple_choice", "When preparing real-time sales data from Azure SQL Database that requires instant updates but involves millions of rows, which storage mode should you select?", [("Import mode for maximum performance", 0), ("DirectQuery mode to maintain real-time data freshness", 1), ("Dual mode for all scenarios", 0), ("Live Connection for external data", 0)], "DirectQuery connects directly to the source, ensuring real-time data accuracy. While Import mode is faster, it cannot provide instant updates—Dual mode is less suitable here without specific aggregation tables."), ("multiple_choice", "You have a Power Query step that filters sales data by year and region from a SQL Server source. You want to verify that query folding is occurring. What should you do?", [("Check the data preview pane for row count", 0), ("Right-click the step in Applied Steps and select &quot;View Native Query&quot; to see if SQL is generated", 1), ("Enable formula bar editing automatically", 0), ("Refresh the data to confirm performance", 0)], "The &quot;View Native Query&quot; option reveals whether the transformation is pushed to SQL Server. If this option is grayed out, folding is not occurring and the full dataset is loaded locally."), ("multiple_choice", "You need to combine sales data from three regional Excel files into a single table for analysis. What operation should you use?", [("Merge the files using a unique key column", 0), ("Append the files by stacking rows vertically", 1), ("Unpivot to consolidate regions", 0), ("Group By region to aggregate", 0)], "Append stacks rows from multiple sources vertically when they have matching column structures. Merge would combine columns horizontally using a key, which is not needed here since each file has the same regional structure."), ("multiple_choice", "A dataset contains customer transaction data in wide format where each month is a separate column (Jan_Sales, Feb_Sales, etc.). What transformation should you apply?", [("Promote Headers to restructure the format", 0), ("Unpivot to convert months from columns into rows for normalized analysis", 1), ("Remove Duplicates to clean monthly data", 0), ("Split Columns to separate the prefix from month names", 0)], "Unpivot transforms wide formats into tall formats, converting month columns into attribute rows. This normalized structure is ideal for time-series analysis and aggregations in Power BI."), ("multiple_choice", "When profiling a dataset in Power Query, you notice that a numeric column shows 85% valid values, 10% null values, and 5% errors. What should be your first step?", [("Immediately delete the entire column due to errors", 0), ("Investigate the error rows to understand their source and determine if they should be removed, replaced, or recoded", 1), ("Replace all errors with zero without investigation", 0), ("Change the data type to text to preserve errors", 0)], "Column quality indicators guide investigation. Understanding why errors exist—whether from data entry issues, encoding problems, or system limitations—informs the best remediation strategy rather than blindly removing or replacing them."), ("multiple_choice", "You create a query parameter for a filter date range that multiple queries reference. What is the primary benefit of using parameters?", [("Parameters reduce file size automatically", 0), ("Changing the parameter value updates all dependent queries without manual editing", 1), ("Parameters eliminate the need for data cleaning", 0), ("Parameters improve query folding performance only", 0)], "Query parameters are reusable values that centralize configuration. When the parameter changes, all referencing queries automatically reflect the new value, enabling dynamic solutions and reducing maintenance effort."), ("multiple_choice", "You are removing duplicate rows from a customer table using Remove Duplicates. Which potential pitfall should you be aware of?", [("It always removes all duplicate values, even if some are legitimate business records", 0), ("It may remove rows that appear identical but represent different transactions; ensuring all key columns are included before deduplication is essential", 1), ("It replaces duplicates with averages instead of removing them", 0), ("It requires that all columns have the same data type", 0)], "Remove Duplicates compares entire rows across all selected columns. If key differentiating columns are excluded, the operation may incorrectly remove legitimate business records. Always verify your column selection before applying this step."), ("multiple_choice", "You need to combine customer details with their order history from two separate tables where each customer may have multiple orders. Which operation is most appropriate?", [("Append the tables vertically to stack all records", 0), ("Merge the tables using a one-to-many relationship on the customer ID key", 1), ("Group By customer ID to consolidate all orders", 0), ("Unpivot the tables to normalize the structure", 0)], "Merge with a one-to-many join correctly combines customer details with multiple orders per customer. Append would duplicate customer information; Group By would aggregate rather than combine the detailed datasets.")]
