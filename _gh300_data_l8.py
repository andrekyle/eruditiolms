# Auto-generated by _build_gh300.py
LESSON_HTML = """
<h2>Lesson 8: Configure Privacy, Content Exclusions, and Safeguards</h2>

<h3>Overview</h3>
<p>GitHub Copilot operates in a complex ecosystem of privacy controls, content restrictions, and security mechanisms. This lesson covers the configuration landscape across user, organization, and enterprise levels—from privacy preferences that govern data usage to technical exclusions that prevent sensitive code from being indexed. You will learn how to troubleshoot common issues, audit activity, and ensure your deployment complies with regulatory requirements.</p>

<h3>Privacy Settings: User-Level Configuration</h3>
<p><strong>Suggestions Matching Public Code</strong> is a user-level setting that, when enabled, allows GitHub Copilot to suggest completions that match code found in public repositories. This setting appears in your Copilot settings or IDE preferences (VS Code, JetBrains, Neovim, etc.). When disabled, Copilot filters out suggestions that have high similarity to public code, reducing the risk of unintended attribution or licensing concerns. The feature is enabled by default for most users.</p>

<p><strong>Telemetry &amp; Product Improvement</strong>: Users can control whether their code snippets, prompts, and completion acceptance data are shared with GitHub for model improvement and product analytics. This setting is typically found under user preferences. When enabled, GitHub uses de-identified usage patterns to improve Copilot. Disabling it restricts GitHub to only basic telemetry (e.g., feature adoption metrics) without access to code context. Enterprise administrators can enforce telemetry policies at the organization level.</p>

<h3>Privacy Settings: Plan-Level Differences</h3>
<p>Privacy and compliance capabilities vary significantly across subscription tiers:</p>

<ul>
<li><strong>Copilot Free &amp; Pro (Individual):</strong> Basic privacy controls; suggestions matching public code is a per-user toggle; telemetry is aggregated and de-identified; no IP indemnity; limited audit visibility.</li>
<li><strong>Copilot Business:</strong> Organization-level content exclusions; admin controls for telemetry; <em>IP indemnity available</em> when public code filter is enabled; audit logs with seat provisioning events; admin dashboard for usage tracking; compliance with organizational data residency if hosted in sovereign regions.</li>
<li><strong>Copilot Enterprise:</strong> All Business features plus: custom model finetuning within private data centers (some scenarios); advanced audit retention; SAML/SSO/SCIM integration; dedicated security documentation and compliance reviews; data residency guarantees; priority support for regulatory inquiries.</li>
</ul>

<h3>Content Exclusions: Scope and Mechanism</h3>
<p>Content exclusions prevent Copilot from using specified files or directories as context when generating suggestions. This is a <em>two-part safeguard</em>: excluded files are not analyzed as context, and matching completions are suppressed.</p>

<p><strong>Organization-Level Exclusions:</strong> Admins define a <code>.github/copilot-excludes.txt</code> file (or equivalent configuration) at the organization level. All repositories within the organization inherit these rules unless explicitly overridden.</p>

<p><strong>Repository-Level Exclusions:</strong> Individual repositories can specify `.github/copilot-excludes.txt` at the repo root. Repo-level rules merge with org-level rules; repo rules take precedence in case of conflict.</p>

<p><strong>Glob Pattern Syntax:</strong> Exclusion rules use standard glob patterns:</p>

<pre><code class="language-yaml"># Example .github/copilot-excludes.txt
**/*.key
**/*.pem
secrets/
migrations/
node_modules/
*.sql
build/
dist/
*.whl
**/*test.py</code></pre>

<p><strong>What Gets Excluded:</strong> When a file matches an exclusion pattern, Copilot will not:</p>
<ol>
<li>Use that file's content as context for generating suggestions.</li>
<li>Return completions that closely match excluded file content.</li>
<li>Index excluded files for semantic search or Copilot Chat context retrieval.</li>
</ol>

<p><strong>Exclusion Propagation:</strong> Exclusions are evaluated at suggestion-generation time. If a user opens an excluded file and invokes Copilot, Copilot will not generate suggestions for that file. Chat may warn the user that certain files are excluded from context. Exclusions do not prevent users from viewing or editing files—they only restrict Copilot's ability to use them.</p>

<h3>Public Code Matching Filter</h3>
<p>The <strong>public code matching filter</strong> (also called "public code filter" or "suggestions matching public code" in some contexts) is a safeguard available in Business and Enterprise plans. When enabled (and paired with IP indemnity), it prevents Copilot from suggesting code that has a near-identical match in public repositories on GitHub.</p>

<p><strong>What It Returns to Users:</strong> When a potential suggestion matches public code:</p>
<ul>
<li>The suggestion is <em>filtered</em> and not offered to the user.</li>
<li>In some IDEs, Copilot may display a notice: "Similar code exists in public repositories."</li>
</ul>

<p><strong>What It Returns to Admins:</strong> Administrators with access to the Copilot analytics dashboard can see:</p>
<ul>
<li>Aggregate counts of filtered suggestions per user or repository.</li>
<li>Trends in public code matches (to identify risky patterns or dependencies on public solutions).</li>
<li>Attribution data for matched repositories (owner, license, URL).</li>
</ul>

<p><strong>How Attribution Works:</strong> When a suggestion is matched against public code, GitHub logs which public repository the code originated from. This metadata is used to inform admins and, in some cases, to attribute and credit original authors when those suggestions are declined or logged.</p>

<h3>IP Indemnity</h3>
<p><strong>When It Applies:</strong> IP indemnity is a legal commitment by GitHub to cover certain intellectual property claims related to Copilot-generated code. It is available only to <strong>Business and Enterprise plan holders</strong> and <em>requires that the public code filter is enabled</em>.</p>

<p><strong>Scope:</strong> IP indemnity covers claims that Copilot-generated code infringes a third party's patent, copyright, or trade secret, provided the customer uses Copilot in compliance with the terms of service and enables available safeguards (including the public code filter).</p>

<p><strong>Not Covered:</strong> IP indemnity does not apply if the user disables the public code filter, uses code that matches known open-source licenses in a way that violates license terms, or violates Copilot's acceptable use policy.</p>

<h3>Safeguards: Content Filtering and Prompt Injection</h3>
<p><strong>Content Filtering for Harmful Output:</strong> Copilot Chat and code suggestions are subject to content filters that detect and suppress:</p>
<ul>
<li>Requests to generate malware, ransomware, or exploit code.</li>
<li>Instructions for illegal activities.</li>
<li>Sexually explicit or hateful content.</li>
<li>Personally identifiable information (PII) generation.</li>
</ul>

<p>When a filter is triggered, the user typically sees a message like: "I can't assist with that request" or "This request violates the content policy."</p>

<p><strong>Prompt Injection Mitigations:</strong> Copilot implements defenses against prompt injection attacks, where malicious code or comments in a file attempt to override system instructions. Mitigations include:</p>
<ul>
<li>Separating system context from user-provided code context.</li>
<li>Limiting the influence of comments or docstrings that attempt to override model behavior.</li>
<li>Monitoring for patterns typical of adversarial prompts.</li>
</ul>

<p><strong>Secret Scanning Interactions:</strong> GitHub's secret scanning service integrates with Copilot in some scenarios. If Copilot detects a pattern resembling an API key, database password, or OAuth token, it may:</p>
<ul>
<li>Refuse to complete or suggest the credential.</li>
<li>Log the detection for security audits.</li>
<li>Alert the organization's security team if configured.</li>
</ul>

<h3>Audit Logs for Copilot</h3>
<p>Audit logs record key administrative and licensing events related to Copilot:</p>

<ul>
<li><strong>Seat Changes:</strong> When users are added to or removed from Copilot licenses.</li>
<li><strong>Policy Changes:</strong> When organization admins modify Copilot settings (e.g., enabling public code filter, requiring telemetry consent).</li>
<li><strong>Content Exclusion Changes:</strong> Updates to `.github/copilot-excludes.txt` or organization-level exclusion rules.</li>
<li><strong>Compliance Events:</strong> When admins export audit logs, when data residency is switched, when IP indemnity is activated or deactivated.</li>
</ul>

<p><strong>Retention &amp; Access:</strong> Logs are retained for a period determined by your GitHub plan (typically 90 days for Business, 1 year for Enterprise, longer upon request). Admins export logs via the GitHub API or UI dashboard. Logs are encrypted in transit and at rest.</p>

<h3>Enterprise-Level Network &amp; Authentication</h3>
<p><strong>Required Endpoints:</strong> Copilot communicates with these primary endpoints:</p>
<ul>
<li><code>api.github.com</code> — authentication, seat provisioning.</li>
<li><code>copilot-proxy.githubusercontent.com</code> — suggestion and chat inference.</li>
<li><code>github.copilotapi.com</code> — Copilot API endpoints.</li>
<li>Logging and telemetry endpoints (regional, depending on residency settings).</li>
</ul>

<p><strong>Network Allowlisting:</strong> Organizations with strict firewall policies must allowlist these domains. If behind a corporate proxy, configure the proxy in IDE settings or via environment variables (<code>HTTP_PROXY</code>, <code>HTTPS_PROXY</code>).</p>

<p><strong>SAML/SSO/SCIM Integration:</strong> Copilot Enterprise supports:</p>
<ul>
<li><strong>SAML 2.0:</strong> Single sign-on; identity federation with corporate directories.</li>
<li><strong>SCIM 2.0:</strong> Automated user provisioning and deprovisioning; group synchronization.</li>
<li><strong>GitHub Organization SCIM API:</strong> Sync Copilot seats with corporate identity management systems.</li>
</ul>

<p>Setup involves configuring your identity provider (Okta, Azure AD, etc.) and linking it to your GitHub organization. Once enabled, user authentication and seat allocation can be automated.</p>

<h3>Compliance and Data Residency</h3>
<p><strong>Data Residency:</strong> Enterprise plans offer data residency options in select regions (e.g., EU, US, APAC). Copilot data—including code context, completions, and logs—is stored and processed within the selected region, ensuring compliance with GDPR, CCPA, and similar regulations.</p>

<p><strong>GDPR Compliance:</strong> Under GDPR, users have rights to:</p>
<ul>
<li><strong>Access:</strong> Obtain a copy of data Copilot has collected about them.</li>
<li><strong>Erasure:</strong> Request deletion of data (subject to legal holds and audit requirements).</li>
<li><strong>Portability:</strong> Export data in a structured format.</li>
</ul>

<p>GitHub processes GDPR requests via its privacy portal. Copilot data deletion may take 30–90 days.</p>

<p><strong>Data Processing Agreements (DPAs):</strong> For Business and Enterprise customers subject to GDPR or similar laws, GitHub offers a DPA that outlines:</p>
<ul>
<li>Data categories and purposes.</li>
<li>Processor obligations and sub-processor lists.</li>
<li>Data subject rights.</li>
<li>Incident notification timelines.</li>
</ul>

<p><strong>What NOT to Paste into Chat:</strong> Users must avoid pasting into Copilot Chat:</p>
<ul>
<li>Personal data (names, emails, phone numbers, SSNs).</li>
<li>Trade secrets or confidential business logic (unless organization has approved data residency).</li>
<li>API keys, passwords, or credentials.</li>
<li>Proprietary or licensed code.</li>
</ul>

<p>Copilot may retain chat history for product improvement (subject to user privacy settings); pasting sensitive data can violate compliance obligations.</p>

<h3>Troubleshooting Copilot Issues</h3>
<p>This table maps common symptoms to likely causes and fixes:</p>

<table>
<thead>
<tr>
<th>Symptom</th>
<th>Likely Cause</th>
<th>Fix</th>
</tr>
</thead>
<tbody>
<tr>
<td>Copilot silent (no suggestions) in a file</td>
<td>File excluded via glob pattern; language not supported; auth token expired</td>
<td>Check `.github/copilot-excludes.txt`; verify language support (Python, JavaScript, TypeScript, Java, Go, C#, C++); sign out and re-authenticate in IDE</td>
</tr>
<tr>
<td>Chat returns "Model unavailable"</td>
<td>Seat expired; plan downgraded; region/model not available in your area</td>
<td>Verify Copilot license is active; check subscription billing; confirm data residency region supports Chat</td>
</tr>
<tr>
<td>Chat errors or timeouts</td>
<td>Network/proxy issues; firewall blocking endpoints; rate limiting</td>
<td>Test connectivity to <code>copilot-proxy.githubusercontent.com</code>; configure proxy settings if behind corporate firewall; wait before retrying</td>
</tr>
<tr>
<td>Suggestions appear very generic or unhelpful</td>
<td>Context too small; language not well-trained; public code filter suppressing good matches</td>
<td>Provide more surrounding code; use a well-supported language; check if filter is enabled and review filtered counts in admin dashboard</td>
</tr>
<tr>
<td>IDE plugin won't start or crashes on launch</td>
<td>Extension version incompatible; corrupted auth cache; missing dependencies</td>
<td>Update extension to latest version; clear IDE extension cache; check IDE logs; reinstall if needed</td>
</tr>
<tr>
<td>Organization sees high "suggestions filtered" count</td>
<td>Repository mirrors or forks of public code; team reuses common patterns from public libraries</td>
<td>Review filtered suggestion trends in admin dashboard; update content exclusions to cover generated or templated code; educate team on licensed vs. proprietary patterns</td>
</tr>
</tbody>
</table>

<h3>Diagnostic Commands</h3>
<p>Use the GitHub CLI to inspect Copilot configuration and audit logs:</p>

<pre><code class="language-bash"># Check Copilot seat status
gh copilot seat

# View organization Copilot settings
gh api /orgs/YOUR_ORG/settings/copilot --jq '.policies'

# List recent audit log entries related to Copilot
gh api /orgs/YOUR_ORG/audit-log --jq '.audit_log[] | select(.action | contains("copilot"))'

# Export audit logs to a file for compliance review
gh api /orgs/YOUR_ORG/audit-log > audit_export.json</code></pre>

<h3>Safeguards Configuration Summary</h3>
<p>This table summarizes where each safeguard is configured and its default state:</p>

<table>
<thead>
<tr>
<th>Safeguard</th>
<th>Configured At (User / Org / Enterprise)</th>
<th>Default</th>
<th>Impact</th>
</tr>
</thead>
<tbody>
<tr>
<td>Suggestions matching public code filter</td>
<td>User (with Org policy override)</td>
<td>Off (Individual); can be mandated On (Business/Enterprise)</td>
<td>Filters suggestions matching public repos; enables IP indemnity when on</td>
</tr>
<tr>
<td>Telemetry &amp; product improvement consent</td>
<td>User (with Org policy)</td>
<td>On (Individual); Org policy overrides (Business+)</td>
<td>Allows GitHub to use code snippets for model improvement</td>
</tr>
<tr>
<td>Content exclusions (glob patterns)</td>
<td>Org / Repo</td>
<td>Empty (no exclusions)</td>
<td>Prevents files matching patterns from being indexed or used as context</td>
</tr>
<tr>
<td>Secret scanning integration</td>
<td>Org (via GitHub Secret Scanning settings)</td>
<td>Off (must be explicitly enabled)</td>
<td>Copilot detects and suppresses credential-like patterns</td>
</tr>
<tr>
<td>Content filtering (harmful output)</td>
<td>Enterprise (model-level)</td>
<td>On</td>
<td>Suppresses malware, exploits, hateful, or PII generation requests</td>
</tr>
<tr>
<td>SAML/SSO/SCIM authentication</td>
<td>Enterprise (Org settings)</td>
<td>Off (GitHub-only auth default)</td>
<td>Integrates Copilot seat provisioning with corporate identity management</td>
</tr>
<tr>
<td>Data residency</td>
<td>Enterprise (plan-level)</td>
<td>US (default); EU, APAC available by contract</td>
<td>Restricts where code and logs are stored and processed</td>
</tr>
<tr>
<td>IP indemnity</td>
<td>Business / Enterprise (contract term)</td>
<td>Off (requires opt-in and active public code filter)</td>
<td>GitHub covers IP infringement claims when enabled</td>
</tr>
</tbody>
</table>

<h3>Best Practices</h3>
<ul>
<li><strong>Enable the public code filter</strong> in Business/Enterprise plans to reduce legal risk and unlock IP indemnity.</li>
<li><strong>Define content exclusions early.</strong> Review sensitive directories (secrets, keys, migrations, tests) and add them to `.github/copilot-excludes.txt` before deployment.</li>
<li><strong>Educate users on what not to paste into Chat.</strong> Regular training sessions reduce the risk of accidental data leaks.</li>
<li><strong>Monitor audit logs.</strong> Set up alerts for policy changes, seat additions, and high rates of filtered suggestions.</li>
<li><strong>Implement SAML/SSO for Enterprise.</strong> Centralized authentication simplifies compliance and reduces onboarding overhead.</li>
<li><strong>Use data residency if subject to GDPR or CCPA.</strong> Ensure code and logs remain within regulated regions.</li>
<li><strong>Test network connectivity and proxy settings</strong> before rolling out Copilot to users behind corporate firewalls.</li>
</ul>
"""

QUESTIONS = [
    (
        "multiple_choice",
        "You are a Business plan admin. An employee asks: \"Can I turn off the public code matching filter so I get more suggestions?\" What should you tell them, and why?",
        [
            ("Yes, it is a personal choice; each user can disable it.", 0),
            ("No, because disabling it will void IP indemnity protection and expose the company to IP infringement claims.", 1),
            ("Yes, but they will lose telemetry rights with GitHub.", 0),
            ("No, because it is an enterprise-only feature.", 0),
        ],
        "The public code filter is essential to IP indemnity. On Business plans, disabling the filter removes indemnity coverage."
    ),
    (
        "multiple_choice",
        "Your organization has a repository with sensitive API credentials in a config file. You want to prevent Copilot from using this file as context and from suggesting code that matches it. Which approach is most effective?",
        [
            ("Ask all developers never to open the file in an IDE with Copilot.", 0),
            ("Add the file path to `.github/copilot-excludes.txt` in the repository root.", 1),
            ("Revoke all Copilot seats and re-issue them with stricter permissions.", 0),
            ("Delete the file and store credentials in a separate secure vault outside the repo.", 0),
        ],
        "Content exclusions via glob patterns in `.github/copilot-excludes.txt` are the intended mechanism. The file is still accessible; Copilot simply ignores it."
    ),
    (
        "multiple_choice",
        "A user in your Enterprise deployment complains that Copilot Chat is returning \"Model unavailable\" errors. You check the admin dashboard and see the user has an active seat. What is the most likely cause?",
        [
            ("The user is not in a supported region for data residency.", 0),
            ("The IDE plugin needs to be updated.", 0),
            ("The user is behind a proxy and cannot reach the required endpoints like copilot-proxy.githubusercontent.com.", 1),
            ("The user has violated the content policy and been rate-limited.", 0),
        ],
        "\"Model unavailable\" with an active seat typically indicates a network or authentication issue, often due to firewall/proxy blocking the Copilot API endpoints."
    ),
    (
        "multiple_choice",
        "Your organization handles GDPR-regulated customer data. A developer is tempted to paste sample code and data into Copilot Chat to get help debugging. What should your security policy require?",
        [
            ("Allow it, because Copilot Chat is encrypted in transit.", 0),
            ("Prohibit it, because Chat history may be retained for product improvement and violate GDPR data processing obligations.", 1),
            ("Allow it if the user is in an EU data residency region.", 0),
            ("Allow it only if the user disables telemetry.", 0),
        ],
        "Even with encryption and GDPR-compliant agreements, pasting regulated data into Chat can violate data minimization principles and data processing agreements. Best practice: never paste PII or customer data."
    ),
    (
        "multiple_choice",
        "You are setting up Copilot for an organization with a corporate proxy. Which of the following must you configure to allow Copilot to function?",
        [
            ("SAML 2.0 federation with the proxy vendor.", 0),
            ("Allowlist the copilot-proxy.githubusercontent.com domain and configure HTTP_PROXY environment variables in IDEs.", 1),
            ("A separate Copilot API token for each developer.", 0),
            ("Custom finetuning models to work behind the proxy.", 0),
        ],
        "Proxy configurations require both network allowlisting and IDE-level proxy settings (environment variables or IDE preferences)."
    ),
    (
        "multiple_choice",
        "A manager asks: \"What audit events does GitHub log for Copilot?\" Which answer is most complete?",
        [
            ("Only suggestions accepted by users and completions rejected.", 0),
            ("Seat changes, policy changes, content exclusion updates, and compliance events like data residency switches.", 1),
            ("Every individual suggestion generated, per user per day.", 0),
            ("Login attempts and IP addresses of all Copilot users.", 0),
        ],
        "Audit logs capture administrative actions: seat changes, policy edits, exclusion rule updates, and compliance events—not individual suggestions."
    ),
    (
        "multiple_choice",
        "You notice your organization has a very high rate of \"suggestions filtered\" in the admin dashboard. What is the most actionable first step?",
        [
            ("Disable the public code filter to show more suggestions.", 0),
            ("Review trends in the dashboard to identify which repositories or teams have high filter rates, then investigate if they are using public code or templates.", 1),
            ("Downgrade all users to Copilot Free to avoid the filtering overhead.", 0),
            ("Disable telemetry to reduce the amount of data being analyzed.", 0),
        ],
        "High filter rates indicate the team may be reusing public code patterns. Investigating trends helps you address root causes, such as outdated public templates or library patterns."
    ),
    (
        "multiple_choice",
        "Your Enterprise customer requires both IP indemnity and compliance with GDPR. Which two actions must they take?",
        [
            ("Enable the public code filter AND enable SAML 2.0 authentication.", 0),
            ("Enable the public code filter AND select a EU data residency region.", 1),
            ("Disable telemetry AND disable content exclusions.", 0),
            ("Enable SCIM provisioning AND increase audit log retention.", 0),
        ],
        "IP indemnity requires the public code filter. GDPR compliance requires data residency in a compliant region (e.g., EU). Both are necessary for the customer's requirements."
    ),
]
